# Escaped quotes and weird characters inside strings
s escape1 = "This string ends with an escaped slash \\";
s escape2 = "What happens to a newline \n inside a string?";
s escape3 = "A string with a \" quote \" in the middle";

# Concatenation syntax check (if your language allows it)
s concat_test = "Hello " + "World";
s str = concat_test + " abc";
s mixed_concat = "Value: " + 42;